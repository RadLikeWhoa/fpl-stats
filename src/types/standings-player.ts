export type StandingsPlayer = {
    id: number
    event_total: number
    player_name: string
    rankg: number
    last_rank: number
    rank_sort: number
    total: number
    entry: number
    entry_number: string
}
